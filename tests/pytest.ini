[pytest]
# pytest 설정 파일
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 출력 설정
addopts = 
    -v
    --tb=short
    --strict-markers

# 마커 정의
markers =
    integration: 통합 테스트 (AWS 리소스 필요)
    property: 속성 기반 테스트 (hypothesis 사용)
    slow: 느린 테스트
